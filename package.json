{
    "name": "tracking-project-root",
    "version": "1.0.0",
    "description": "Root script to run server and client",
    "scripts": {
        "dev": "npm run dev --prefix server",
        "install-all": "npm install --prefix server && npm install --prefix client"
    }
}